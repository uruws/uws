#!/bin/sh
set -eu
cd /srv/uws/deploy
exec ./docker/k8s/cli.sh amy-wrkr ./k8s/autoscaler/logs.sh $@

#uwscli:worker-autoscaler-logs	- get meteor worker autoscaler logs

#uwsdoc:worker-autoscaler-logs [-f] [--tail=N]
#uwsdoc:
#uwsdoc:  -f       - follow the output (wait for and show new entries).
#uwsdoc:  --tail=N - get latest N log lines.
