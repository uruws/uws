#!/bin/sh
set -eu
export NQDIR=${HOME}/.nq
exec fq $@

#uwscli:uwsq - show information about uws tasks queue

#uwsdoc:uwsq
#uwsdoc:
#uwsdoc:	It will show current running job output or information about last
#uwsdoc:    failed task. If there's no output, there are no running tasks nor
#uwsdoc:	any that failed either.
