#!/bin/sh
set -eu
release=${1:?'app release?'}
uwsnq /srv/deploy/Buildpack/build.sh ${release}
exit 0

#uwscli:meteor-build		- build meteor app version/tag

#uwsdoc:meteor-build X.Y.Z
#uwsdoc:
#uwsdoc:  Build meteor app from X.Y.Z git tag.
