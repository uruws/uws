#!/bin/sh
set -eu
cd /srv/uws/deploy
exec ./docker/k8s/cli.sh amybeta ./k8s/autoscaler/logs.sh $@

#uwscli:beta-autoscaler-logs		- get meteor beta autoscaler logs

#uwsdoc:beta-autoscaler-logs [-f] [--tail=N]
#uwsdoc:
#uwsdoc:  -f       - follow the output (wait for and show new entries).
#uwsdoc:  --tail=N - get latest N log lines.
