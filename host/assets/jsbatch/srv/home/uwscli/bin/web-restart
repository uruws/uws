#!/bin/sh
set -eu
region=${1:?'east or west?'}
uwsnq /srv/uws/deploy/cli/meteor-ctl.sh "amy-${1}" web restart
exit 0

#uwscli:web-restart - restart meteor web deployment

#uwsdoc:web-restart
#uwsdoc:
#uwsdoc:  Restart meteor web deployment.
