---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ${INGRESS_NAME}
  annotations:
    nginx.ingress.kubernetes.io/default-backend: "offline-page-${APP_NAMESPACE}"
    nginx.ingress.kubernetes.io/custom-http-errors: "500,502,503,504"
