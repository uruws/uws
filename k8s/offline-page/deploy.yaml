---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: offline-page
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: offline-page
  replicas: ${APP_REPLICAS}
  revisionHistoryLimit: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 50%
      maxSurge: 50%
  template:
    metadata:
      labels:
        app.kubernetes.io/name: offline-page
    spec:
      terminationGracePeriodSeconds: 5
      containers:
        - image: 789470191893.dkr.ecr.${AWS_REGION}.amazonaws.com/uws:offline-page-${APP_VERSION}
          imagePullPolicy: IfNotPresent
          name: offline-page
          ports:
            - containerPort: 8008
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
          env:
            - name: UWS_DEBUG
              value: "off"
          resources:
            requests:
              cpu: 10m
              memory: 100Mi
            limits:
              cpu: 10m
              memory: 100Mi
          readinessProbe:
            exec:
              command:
                - cat
                - /uws/offlinepage/index.html
            initialDelaySeconds: 5
            periodSeconds: 15
            timeoutSeconds: 3
---
apiVersion: v1
kind: Service
metadata:
  name: offline-page
spec:
  ports:
    - port: 8000
      targetPort: 8008
      protocol: TCP
  selector:
    app.kubernetes.io/name: offline-page
