# nginx test site conf

server {
	listen 80;
	server_name localhost;

	include snippets/uwslog-json.conf;

	set $upstream_name "testing-global";

	location / {
		set $upstream_name "testing";
		include snippets/proxy_params;
	}
}
