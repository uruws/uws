---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: podtest
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: podtest
  replicas: 5
  template:
    metadata:
      labels:
        app.kubernetes.io/name: podtest
    spec:
      containers:
        - image: 789470191893.dkr.ecr.${AWS_REGION}.amazonaws.com/uws:podtest-${APP_VERSION}
          imagePullPolicy: IfNotPresent
          name: podtest
          ports:
            - containerPort: 8699
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsUser: 3000
            runAsGroup: 3000
