---
apiVersion: v1
kind: Service
metadata:
  name: meteor-web-service
spec:
  type: ExternalName
  externalName: meteor-web-service.meteor-web.svc.cluster.local
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: meteor-web-gateway
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: "HTTP"
    nginx.ingress.kubernetes.io/rewrite-target: /$1
    cert-manager.io/issuer: acme-prod
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - test.uws.talkingpts.org
      secretName: meteor-web-certificate
  rules:
    - host: test.uws.talkingpts.org
      http:
        paths:
          - pathType: Prefix
            path: /(.*)
            backend:
              service:
                name: meteor-web-service
                port:
                  number: 3000
